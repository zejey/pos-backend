# Core
Django~=5.2.13
djangorestframework~=3.16.0
djangorestframework-simplejwt~=5.3

# Database driver (PostgreSQL)
psycopg[binary]~=3.2

# API tooling
django-cors-headers~=4.6
django-filter~=24.3
drf-spectacular~=0.28.0

# Required by DRF's test client (rest_framework.test imports `requests` at
# module load, pulled in via conftest.py and drf-spectacular). Pinned
# explicitly because a missing `requests`/`urllib3` stops the server from
# starting and breaks pytest collection.
requests~=2.32
urllib3~=2.0

# Production server
gunicorn~=23.0
whitenoise~=6.8
